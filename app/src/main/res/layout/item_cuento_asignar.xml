<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginVertical="8dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="4dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="16dp"
        android:background="?android:attr/selectableItemBackground">

        <!-- Imagen del cuento -->
        <ImageView
            android:id="@+id/imagen_cuento_asignar"
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:layout_marginEnd="16dp"
            android:background="@drawable/imagen_cuento_placeholder"
            android:scaleType="centerCrop"
            android:contentDescription="@string/cuentos_asignados" />

        <!-- Contenido del cuento -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <!-- Título del cuento -->
            <TextView
                android:id="@+id/texto_titulo_cuento_asignar"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Caperucita Roja"
                android:textColor="@color/gris_oscuro"
                android:textSize="18sp"
                android:textStyle="bold" />

            <!-- Subtítulo/Autor -->
            <TextView
                android:id="@+id/texto_subtitulo_cuento_asignar"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:text="Cuento tradicional europeo"
                android:textColor="@color/gris_medio"
                android:textSize="14sp" />

            <!-- Badge de edad -->
            <TextView
                android:id="@+id/badge_edad_cuento"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:text="4-6"
                android:textColor="@color/gris_oscuro"
                android:textSize="12sp"
                android:background="@drawable/badge_edad"
                android:paddingHorizontal="8dp"
                android:paddingVertical="4dp" />

            <!-- Tiempo de lectura -->
            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:orientation="horizontal"
                android:gravity="center_vertical">

                <ImageView
                    android:layout_width="16dp"
                    android:layout_height="16dp"
                    android:src="@drawable/ic_clock"
                    android:tint="@color/gris_medio"
                    android:layout_marginEnd="4dp"
                    android:contentDescription="@string/tiempo_lectura" />

                <TextView
                    android:id="@+id/texto_tiempo_lectura"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="15 min"
                    android:textColor="@color/gris_medio"
                    android:textSize="13sp" />

            </LinearLayout>

        </LinearLayout>

        <!-- Botones de acción -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="center">

            <Button
                android:id="@+id/boton_asignar_cuento"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/asignar"
                android:textColor="@android:color/white"
                android:background="@drawable/boton_verde"
                android:paddingHorizontal="16dp"
                android:paddingVertical="8dp"
                android:textSize="14sp"
                android:textStyle="bold"
                app:backgroundTint="@null" />

            <ImageView
                android:id="@+id/icono_vista_previa"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:layout_marginTop="8dp"
                android:src="@drawable/ic_eye"
                android:tint="@color/gris_medio"
                android:contentDescription="@string/vista_previa"
                android:padding="4dp" />

        </LinearLayout>

    </LinearLayout>

</androidx.cardview.widget.CardView>
